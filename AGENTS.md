Always on any change build ARM64 apk with bumped version 2.0.x, at /output (project root/output)
Sign that APK with v2raynu-release.jks
Always remove the previous APK if there was at /output directory
